{
  "name": "basic-javascript",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "json-server -w db.json --port 8000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Chaya Phuwichit",
  "license": "ISC",
  "dependencies": {
    "http-server": "^14.1.1"
  },
  "devDependencies": {
    "json-server": "^1.0.0-beta.1"
  }
}
